<MudDrawer @bind-Open="@DrawerOpen" ClipMode="@DrawerClipMode.Always" Elevation="0" Variant="@DrawerVariant.Responsive">
    <MudDrawerHeader>
        <MudText Typo="@Typo.h6">Meddy Explorer</MudText>
    </MudDrawerHeader>
    <MudNavMenu>
        <MudNavLink Match="@NavLinkMatch.All">
            <MudText Typo="@Typo.body1">
                Store
            </MudText>
        </MudNavLink>
        <MudNavLink Match="@NavLinkMatch.All">
            <MudText Typo="@Typo.body1">
                Library
            </MudText>
        </MudNavLink>
        <MudNavLink Match="@NavLinkMatch.All">
            <MudText Typo="@Typo.body1">
                Community
            </MudText>
        </MudNavLink>
    </MudNavMenu>
</MudDrawer>

@code
{
    [Parameter]
    public bool DrawerOpen { get; set; }
}
